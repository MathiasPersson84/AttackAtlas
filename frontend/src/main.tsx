import React,{useEffect,useMemo,useRef,useState} from 'react';
import {createRoot} from 'react-dom/client';
import {Network,Plus,Upload,Server,Monitor,Router,Box,Apple,Link2,MousePointer2,Trash2,X,ScanLine,ZoomIn,ZoomOut,Maximize2,Hand,PanelRightClose,PanelRightOpen,StickyNote,Activity,ArrowRight,RotateCcw,KeyRound,Eye,EyeOff,Copy,Edit3,ChevronRight,ChevronDown,UserPlus,Users,FileUp,Save,Unlink,MoreHorizontal,Pencil,Settings,Download,FileText} from 'lucide-react';
import './styles.css';

type Project={id:number,name:string,description?:string,notes?:string};
type Service={id:number,protocol:string,port:number,state:string,name:string,product:string,version:string,extra_info:string,script_output:string,raw_output:string};
type Host={id:number,address:string,hostname:string,os:string,os_family:string,device_type:string,status:string,notes:string,pos_x:number,pos_y:number,services:Service[]};
type Edge={id:number,source_type:string,source_id:number,target_type:string,target_id:number,relation:string,label:string,directed:boolean};
type Account={id:number,username:string,domain:string,notes:string,host_id:number|null};
type Credential={id:number,account_id:number|null,kind:string,secret:string,source:string,host_id:number|null,notes:string};
type HostDraft={address:string,hostname:string,os:string,os_family:string,device_type:string,status:string,notes:string};
type HostTemplate={id:string,label:string,os:string,os_family:string,device_type:string};
type DragState={id:number,startClientX:number,startClientY:number,startX:number,startY:number,moved:boolean};
type PanState={startClientX:number,startClientY:number,startX:number,startY:number}|null;
type ContextState={x:number,y:number,hostId:number}|null;
type ConnectDrag={source:number,x:number,y:number}|null;

const emptyHost:HostDraft={address:'',hostname:'',os:'',os_family:'unknown',device_type:'host',status:'discovered',notes:''};
const hostTemplates:HostTemplate[]=[
 {id:'custom',label:'Custom / blank',os:'',os_family:'unknown',device_type:'host'},
 {id:'win-server-2008',label:'Windows Server 2008',os:'Windows Server 2008',os_family:'windows',device_type:'server'},
 {id:'win-server-2012',label:'Windows Server 2012',os:'Windows Server 2012',os_family:'windows',device_type:'server'},
 {id:'win-server-2016',label:'Windows Server 2016',os:'Windows Server 2016',os_family:'windows',device_type:'server'},
 {id:'win-server-2019',label:'Windows Server 2019',os:'Windows Server 2019',os_family:'windows',device_type:'server'},
 {id:'win-server-2022',label:'Windows Server 2022',os:'Windows Server 2022',os_family:'windows',device_type:'server'},
 {id:'win-server-2025',label:'Windows Server 2025',os:'Windows Server 2025',os_family:'windows',device_type:'server'},
 {id:'win-xp',label:'Windows XP',os:'Windows XP',os_family:'windows',device_type:'workstation'},
 {id:'win-7',label:'Windows 7',os:'Windows 7',os_family:'windows',device_type:'workstation'},
 {id:'win-10',label:'Windows 10',os:'Windows 10',os_family:'windows',device_type:'workstation'},
 {id:'win-11',label:'Windows 11',os:'Windows 11',os_family:'windows',device_type:'workstation'},
 {id:'linux-kali',label:'Linux · Kali',os:'Kali Linux',os_family:'linux',device_type:'workstation'},
 {id:'linux-ubuntu',label:'Linux · Ubuntu',os:'Ubuntu Linux',os_family:'linux',device_type:'workstation'},
 {id:'linux-ubuntu-server',label:'Linux · Ubuntu Server',os:'Ubuntu Server',os_family:'linux',device_type:'server'},
];
const NODE_W=268,NODE_H=176,WORLD_W=3200,WORLD_H=2200;
const relations=['access','valid_credentials','smb','winrm','ssh','local_admin','pivot','member_of','dcsync','custom'];
async function api(path:string,init?:RequestInit){const r=await fetch(path,init);if(!r.ok){let t=await r.text();try{t=JSON.parse(t).detail||t}catch{}throw new Error(t)}return r.status===204?null:r.json()}
function HostIcon({host}:{host:Host}){if(host.device_type==='network')return <Router/>;if(host.device_type==='container')return <Box/>;if(host.os_family==='macos')return <Apple/>;if(host.device_type==='workstation')return <Monitor/>;return <Server/>}
function clamp(v:number,min:number,max:number){return Math.max(min,Math.min(max,v))}
function edgePath(a:Host,b:Host){
 const ac={x:a.pos_x+NODE_W/2,y:a.pos_y+NODE_H/2},bc={x:b.pos_x+NODE_W/2,y:b.pos_y+NODE_H/2};
 const dx=bc.x-ac.x,dy=bc.y-ac.y;let x1=ac.x,y1=ac.y,x2=bc.x,y2=bc.y,side:'h'|'v'='h';
 if(Math.abs(dx)>=Math.abs(dy)){x1=a.pos_x+(dx>=0?NODE_W:0);y1=ac.y;x2=b.pos_x+(dx>=0?0:NODE_W);y2=bc.y;side='h'}
 else{y1=a.pos_y+(dy>=0?NODE_H:0);x1=ac.x;y2=b.pos_y+(dy>=0?0:NODE_H);x2=bc.x;side='v'}
 const bend=clamp((side==='h'?Math.abs(x2-x1):Math.abs(y2-y1))*.45,55,180);
 const d=side==='h'?`M ${x1} ${y1} C ${x1+(dx>=0?bend:-bend)} ${y1}, ${x2+(dx>=0?-bend:bend)} ${y2}, ${x2} ${y2}`:`M ${x1} ${y1} C ${x1} ${y1+(dy>=0?bend:-bend)}, ${x2} ${y2+(dy>=0?-bend:bend)}, ${x2} ${y2}`;
 return {d,mx:(x1+x2)/2,my:(y1+y2)/2};
}

function App(){
 const [projects,setProjects]=useState<Project[]>([]),[pid,setPid]=useState<number|null>(null),[hosts,setHosts]=useState<Host[]>([]),[edges,setEdges]=useState<Edge[]>([]),[accounts,setAccounts]=useState<Account[]>([]),[credentials,setCredentials]=useState<Credential[]>([]);
 const [msg,setMsg]=useState(''),[addOpen,setAddOpen]=useState(false),[draft,setDraft]=useState<HostDraft>(emptyHost),[templateId,setTemplateId]=useState('custom');
 const [projectOpen,setProjectOpen]=useState(false),[projectDraft,setProjectDraft]=useState({name:'',description:'',notes:''});
 const [editHostId,setEditHostId]=useState<number|null>(null),[editDraft,setEditDraft]=useState<HostDraft>(emptyHost);
 const [mode,setMode]=useState<'select'|'connect'|'pan'>('select'),[connectFrom,setConnectFrom]=useState<number|null>(null),[connectDrag,setConnectDrag]=useState<ConnectDrag>(null);
 const [edgeDraft,setEdgeDraft]=useState<{source:number,target:number}|null>(null),[edgeEdit,setEdgeEdit]=useState<Edge|null>(null),[edgeRelation,setEdgeRelation]=useState('access'),[edgeLabel,setEdgeLabel]=useState(''),[edgeDirected,setEdgeDirected]=useState(true),[selectedEdge,setSelectedEdge]=useState<number|null>(null);
 const [selectedHost,setSelectedHost]=useState<number|null>(null),[panelOpen,setPanelOpen]=useState(true),[tab,setTab]=useState<'details'|'services'|'notes'|'credentials'>('details'),[expandedService,setExpandedService]=useState<number|null>(null);
 const [drag,setDrag]=useState<DragState|null>(null),[panDrag,setPanDrag]=useState<PanState>(null),[hostDrop,setHostDrop]=useState<number|null>(null),[globalDrop,setGlobalDrop]=useState(false),[trashHot,setTrashHot]=useState(false);
 const [zoom,setZoom]=useState(1),[pan,setPan]=useState({x:0,y:0}),[ctx,setCtx]=useState<ContextState>(null);
 const [noteHost,setNoteHost]=useState<number|null>(null),[noteText,setNoteText]=useState('');
 const [credOpen,setCredOpen]=useState(false),[credHost,setCredHost]=useState<number|null>(null),[showSecrets,setShowSecrets]=useState<Set<number>>(new Set()),[credentialsExpanded,setCredentialsExpanded]=useState(true),[usersExpanded,setUsersExpanded]=useState(true),[userDropHot,setUserDropHot]=useState(false),[editingCredentialId,setEditingCredentialId]=useState<number|null>(null),[editingAccountId,setEditingAccountId]=useState<number|null>(null);
 const [newAccount,setNewAccount]=useState({username:'',domain:'',notes:'',host_id:''}),[newCred,setNewCred]=useState({account_id:'',kind:'password',secret:'',source:'manual',host_id:'',notes:''});
 const viewportRef=useRef<HTMLDivElement>(null),trashRef=useRef<HTMLDivElement>(null),nmapInputRef=useRef<HTMLInputElement>(null),pendingScanHost=useRef<number|null>(null);

 const byId=useMemo(()=>Object.fromEntries(hosts.map(h=>[h.id,h])),[hosts]);
 const accountById=useMemo(()=>Object.fromEntries(accounts.map(a=>[a.id,a])),[accounts]);
 const usersByHost=useMemo(()=>{const groups=new Map<number|null,Account[]>();for(const a of accounts){const key=a.host_id??null;groups.set(key,[...(groups.get(key)||[]),a])}return groups},[accounts]);
 const selected=selectedHost?byId[selectedHost]:null;
 const totalServices=hosts.reduce((n,h)=>n+h.services.length,0);
 const hostCreds=selected?credentials.filter(c=>c.host_id===selected.id):[];
 const loadProjects=async()=>{const p=await api('/api/v1/projects');setProjects(p);if(!pid&&p[0])setPid(p[0].id)};
 const loadAll=async(id:number)=>{const [h,e,a,c]=await Promise.all([api(`/api/v1/projects/${id}/hosts`),api(`/api/v1/projects/${id}/edges`),api(`/api/v1/projects/${id}/accounts`),api(`/api/v1/projects/${id}/credentials`)]);setHosts(h);setEdges(e);setAccounts(a);setCredentials(c)};
 useEffect(()=>{loadProjects()},[]);useEffect(()=>{if(pid)loadAll(pid)},[pid]);useEffect(()=>{setExpandedService(null)},[selectedHost]);
 useEffect(()=>{const close=()=>setCtx(null);window.addEventListener('click',close);return()=>window.removeEventListener('click',close)},[]);

 async function addProject(){const name=prompt('Project name');if(!name)return;await api('/api/v1/projects',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name})});await loadProjects()}
 function openProjectSettings(){const p=projects.find(x=>x.id===pid);if(!p)return;setProjectDraft({name:p.name,description:p.description||'',notes:p.notes||''});setProjectOpen(true)}
 async function saveProject(e:React.FormEvent){e.preventDefault();if(!pid||!projectDraft.name.trim())return;try{await api(`/api/v1/projects/${pid}`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({...projectDraft,name:projectDraft.name.trim()})});setProjectOpen(false);await loadProjects();setMsg('Project updated')}catch(e:any){setMsg(`Could not update project: ${e.message}`)}}
 async function deleteProject(){if(!pid)return;const p=projects.find(x=>x.id===pid);if(!p)return;if(!confirm(`Delete project ${p.name}? This permanently removes its hosts, services, credentials, shares, scans and attack paths.`))return;try{await api(`/api/v1/projects/${pid}`,{method:'DELETE'});setProjectOpen(false);setHosts([]);setEdges([]);setAccounts([]);setCredentials([]);setSelectedHost(null);setSelectedEdge(null);setPid(null);const rows=await api('/api/v1/projects');setProjects(rows);if(rows[0])setPid(rows[0].id);setMsg(`Deleted project ${p.name}`)}catch(e:any){setMsg(`Could not delete project: ${e.message}`)}}
 function exportMarkdown(){if(!pid)return;if(!confirm('Export all project data to Markdown? The ZIP includes plaintext credential secrets.'))return;window.location.href=`/api/v1/projects/${pid}/export/markdown`}
 async function createHost(e:React.FormEvent){e.preventDefault();if(!pid||!draft.address.trim())return;const n=hosts.length;try{await api(`/api/v1/projects/${pid}/hosts`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...draft,address:draft.address.trim(),hostname:draft.hostname.trim(),pos_x:120+(n%5)*310,pos_y:110+Math.floor(n/5)*230})});setAddOpen(false);setDraft(emptyHost);setTemplateId('custom');await loadAll(pid)}catch(e:any){setMsg(e.message)}}
 function applyTemplate(id:string){setTemplateId(id);const t=hostTemplates.find(x=>x.id===id);if(!t)return;setDraft(v=>({...v,os:t.os,os_family:t.os_family,device_type:t.device_type}))}
 function openHostEditor(h:Host){setEditHostId(h.id);setEditDraft({address:h.address,hostname:h.hostname||'',os:h.os||'',os_family:h.os_family||'unknown',device_type:h.device_type||'host',status:h.status||'discovered',notes:h.notes||''});setCtx(null);setSelectedHost(h.id);setPanelOpen(true)}
 async function saveHostEdit(e:React.FormEvent){e.preventDefault();if(editHostId===null||!editDraft.address.trim())return;try{await patchHost(editHostId,{...editDraft,address:editDraft.address.trim(),hostname:editDraft.hostname.trim()});setEditHostId(null);setMsg('Host updated')}catch(e:any){setMsg(`Could not update host: ${e.message}`)}}
 async function patchHost(id:number,body:any){if(!pid)return;await api(`/api/v1/projects/${pid}/hosts/${id}`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});await loadAll(pid)}
 async function savePosition(id:number,x:number,y:number){try{await patchHost(id,{pos_x:Math.round(x),pos_y:Math.round(y)})}catch(e:any){setMsg(`Could not save position: ${e.message}`)}}
 async function deleteHost(id:number){if(!pid)return;const h=byId[id];if(!h)return;if(!confirm(`Delete ${h.hostname||h.address}? Services, shares and connected attack paths will also be removed.`))return;try{await api(`/api/v1/projects/${pid}/hosts/${id}`,{method:'DELETE'});if(selectedHost===id)setSelectedHost(null);setSelectedEdge(null);await loadAll(pid);setMsg(`Deleted ${h.hostname||h.address}`)}catch(e:any){setMsg(`Could not delete host: ${e.message}`)}}
 function trashContains(x:number,y:number){const r=trashRef.current?.getBoundingClientRect();return !!r&&x>=r.left&&x<=r.right&&y>=r.top&&y<=r.bottom}
 function hostPointerDown(e:React.PointerEvent,h:Host){
  if(e.button!==0||(e.target as HTMLElement).closest('.host-actions')||(e.target as HTMLElement).closest('.port-handle'))return;
  if(mode==='connect'){e.stopPropagation();setSelectedHost(h.id);if(connectFrom===null){setConnectFrom(h.id);setMsg(`Select target for ${h.hostname||h.address}`)}else if(connectFrom!==h.id){openEdgeDraft(connectFrom,h.id);setConnectFrom(null)}return}
  if(mode==='pan')return;e.stopPropagation();(e.currentTarget as HTMLElement).setPointerCapture(e.pointerId);setSelectedHost(h.id);setSelectedEdge(null);setDrag({id:h.id,startClientX:e.clientX,startClientY:e.clientY,startX:h.pos_x,startY:h.pos_y,moved:false})
 }
 function viewportPointerDown(e:React.PointerEvent){if(e.button!==0)return;if((e.target as HTMLElement).closest('.node')||(e.target as HTMLElement).closest('.edge-hit'))return;if(mode==='connect')return;setSelectedHost(null);setSelectedEdge(null);setPanDrag({startClientX:e.clientX,startClientY:e.clientY,startX:pan.x,startY:pan.y});(e.currentTarget as HTMLElement).setPointerCapture(e.pointerId)}
 function pointerMove(e:React.PointerEvent){if(drag){const dx=(e.clientX-drag.startClientX)/zoom,dy=(e.clientY-drag.startClientY)/zoom;const nx=clamp(drag.startX+dx,8,WORLD_W-NODE_W-8),ny=clamp(drag.startY+dy,8,WORLD_H-NODE_H-8);setHosts(v=>v.map(h=>h.id===drag.id?{...h,pos_x:nx,pos_y:ny}:h));setDrag({...drag,moved:drag.moved||Math.abs(dx)>3||Math.abs(dy)>3});setTrashHot(trashContains(e.clientX,e.clientY));return}if(panDrag)setPan({x:panDrag.startX+e.clientX-panDrag.startClientX,y:panDrag.startY+e.clientY-panDrag.startClientY})}
 function pointerUp(e:React.PointerEvent){if(drag){const h=hosts.find(x=>x.id===drag.id),overTrash=trashContains(e.clientX,e.clientY);setDrag(null);setTrashHot(false);if(overTrash&&h){deleteHost(h.id);return}if(h)savePosition(h.id,h.pos_x,h.pos_y)}setPanDrag(null)}
 function openEdgeDraft(source:number,target:number){setEdgeDraft({source,target});setEdgeEdit(null);setEdgeRelation('access');setEdgeLabel('');setEdgeDirected(true)}
 function startConnector(e:React.PointerEvent,h:Host){e.stopPropagation();e.preventDefault();(e.currentTarget as HTMLElement).setPointerCapture(e.pointerId);setConnectDrag({source:h.id,x:h.pos_x+NODE_W/2,y:h.pos_y+NODE_H/2});setSelectedHost(h.id)}
 function moveConnector(e:React.PointerEvent){if(!connectDrag||!viewportRef.current)return;const r=viewportRef.current.getBoundingClientRect();setConnectDrag({...connectDrag,x:(e.clientX-r.left-pan.x)/zoom,y:(e.clientY-r.top-pan.y)/zoom})}
 function endConnector(e:React.PointerEvent){if(!connectDrag)return;const el=document.elementFromPoint(e.clientX,e.clientY) as HTMLElement|null;const node=el?.closest('[data-host-id]') as HTMLElement|null;const target=node?Number(node.dataset.hostId):0;const source=connectDrag.source;setConnectDrag(null);if(target&&target!==source)openEdgeDraft(source,target)}
 async function createEdge(e:React.FormEvent){e.preventDefault();if(!pid||!edgeDraft)return;try{await api(`/api/v1/projects/${pid}/edges`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({source_type:'host',source_id:edgeDraft.source,target_type:'host',target_id:edgeDraft.target,relation:edgeRelation||'access',label:edgeLabel,directed:edgeDirected})});setEdgeDraft(null);setMode('select');await loadAll(pid)}catch(e:any){setMsg(e.message)}}
 function openEdgeEditor(edge:Edge){setEdgeEdit(edge);setEdgeRelation(edge.relation);setEdgeLabel(edge.label);setEdgeDirected(edge.directed)}
 async function updateEdge(e:React.FormEvent){e.preventDefault();if(!pid||!edgeEdit)return;await api(`/api/v1/projects/${pid}/edges/${edgeEdit.id}`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({relation:edgeRelation,label:edgeLabel,directed:edgeDirected})});setEdgeEdit(null);await loadAll(pid)}
 async function deleteEdge(id=selectedEdge){if(!pid||id===null)return;await api(`/api/v1/projects/${pid}/edges/${id}`,{method:'DELETE'});setSelectedEdge(null);setEdgeEdit(null);await loadAll(pid)}
 async function upload(file:File,targetHost?:Host){if(!pid)return;if(!file.name.toLowerCase().endsWith('.xml')){setMsg('Only Nmap XML files are accepted.');return}const fd=new FormData();fd.append('file',file);const path=targetHost?`/api/v1/projects/${pid}/hosts/${targetHost.id}/imports/nmap`:`/api/v1/projects/${pid}/imports/nmap`;try{const r=await api(path,{method:'POST',body:fd});setMsg(targetHost?`Updated ${targetHost.hostname||targetHost.address}: ${r.services_seen} services`:`Imported ${r.hosts_seen} hosts / ${r.services_seen} services`);await loadAll(pid)}catch(e:any){setMsg(`Import failed: ${e.message}`)}}
 function triggerScan(hostId:number|null){pendingScanHost.current=hostId;nmapInputRef.current?.click()}
 function editNotes(h:Host){setNoteHost(h.id);setNoteText(h.notes||'');setCtx(null)}
 async function saveNotes(e:React.FormEvent){e.preventDefault();if(noteHost===null)return;await patchHost(noteHost,{notes:noteText});setNoteHost(null)}
 async function createAccount(e:React.FormEvent){e.preventDefault();if(!pid||!newAccount.username.trim())return;const body={username:newAccount.username.trim(),domain:newAccount.domain.trim(),notes:newAccount.notes,host_id:newAccount.host_id?Number(newAccount.host_id):null};const path=editingAccountId?`/api/v1/projects/${pid}/accounts/${editingAccountId}`:`/api/v1/projects/${pid}/accounts`;try{await api(path,{method:editingAccountId?'PATCH':'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});setMsg(editingAccountId?'User updated':'User added');setEditingAccountId(null);setNewAccount({username:'',domain:'',notes:'',host_id:''});await loadAll(pid)}catch(e:any){setMsg(`Could not save user: ${e.message}`)}}
 function editAccount(a:Account){setEditingAccountId(a.id);setNewAccount({username:a.username,domain:a.domain||'',notes:a.notes||'',host_id:a.host_id?String(a.host_id):''});setCredOpen(true);setCtx(null)}
 function cancelAccountEdit(){setEditingAccountId(null);setNewAccount({username:'',domain:'',notes:'',host_id:''})}
 async function importUsersCsv(file:File){if(!pid)return;if(!file.name.toLowerCase().endsWith('.csv')){setMsg('User import expects a .csv file.');return}const fd=new FormData();fd.append('file',file);try{const r=await api(`/api/v1/projects/${pid}/accounts/import/csv`,{method:'POST',body:fd});setMsg(`Users imported: ${r.created} new, ${r.updated} updated, ${r.skipped} skipped${r.error_count?`, ${r.error_count} errors`:''}`);await loadAll(pid)}catch(e:any){setMsg(`User import failed: ${e.message}`)}}
 async function createCredential(e:React.FormEvent){e.preventDefault();if(!pid||!newCred.secret)return;const body={account_id:newCred.account_id?Number(newCred.account_id):null,kind:newCred.kind,secret:newCred.secret,source:newCred.source,host_id:newCred.host_id?Number(newCred.host_id):null,notes:newCred.notes};const path=editingCredentialId?`/api/v1/projects/${pid}/credentials/${editingCredentialId}`:`/api/v1/projects/${pid}/credentials`;await api(path,{method:editingCredentialId?'PATCH':'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});setNewCred({account_id:'',kind:'password',secret:'',source:'manual',host_id:credHost?String(credHost):'',notes:''});setEditingCredentialId(null);setMsg(editingCredentialId?'Credential updated':'Credential added');await loadAll(pid)}
 async function deleteCredential(id:number){if(!pid||!confirm('Delete this credential?'))return;await api(`/api/v1/projects/${pid}/credentials/${id}`,{method:'DELETE'});if(editingCredentialId===id){setEditingCredentialId(null);setNewCred({account_id:'',kind:'password',secret:'',source:'manual',host_id:credHost?String(credHost):'',notes:''})}await loadAll(pid)}
 function editCredential(c:Credential){setEditingCredentialId(c.id);setNewCred({account_id:c.account_id?String(c.account_id):'',kind:c.kind,secret:c.secret,source:c.source||'',host_id:c.host_id?String(c.host_id):'',notes:c.notes||''});setCredHost(c.host_id||null);setCredOpen(true);setCtx(null)}
 function cancelCredentialEdit(){setEditingCredentialId(null);setNewCred({account_id:'',kind:'password',secret:'',source:'manual',host_id:credHost?String(credHost):'',notes:''})}
 function openCredentials(hostId:number|null=null){setCredHost(hostId);setEditingCredentialId(null);setEditingAccountId(null);setNewAccount({username:'',domain:'',notes:'',host_id:hostId?String(hostId):''});setNewCred({account_id:'',kind:'password',secret:'',source:'manual',host_id:hostId?String(hostId):'',notes:''});setCredOpen(true);setCtx(null)}
 function toggleSecret(id:number){setShowSecrets(s=>{const n=new Set(s);n.has(id)?n.delete(id):n.add(id);return n})}
 async function copySecret(secret:string){try{await navigator.clipboard.writeText(secret);setMsg('Credential copied')}catch{try{const ta=document.createElement('textarea');ta.value=secret;ta.style.position='fixed';ta.style.opacity='0';document.body.appendChild(ta);ta.select();document.execCommand('copy');ta.remove();setMsg('Credential copied')}catch{setMsg('Could not copy credential')}}}
 function zoomAt(next:number){setZoom(clamp(next,.45,1.8))}
 function fitView(){if(!viewportRef.current||hosts.length===0){setPan({x:0,y:0});setZoom(1);return}const minX=Math.min(...hosts.map(h=>h.pos_x)),minY=Math.min(...hosts.map(h=>h.pos_y)),maxX=Math.max(...hosts.map(h=>h.pos_x+NODE_W)),maxY=Math.max(...hosts.map(h=>h.pos_y+NODE_H));const r=viewportRef.current.getBoundingClientRect(),pad=90,z=clamp(Math.min((r.width-pad*2)/(maxX-minX),(r.height-pad*2)/(maxY-minY)),.45,1.35);setZoom(z);setPan({x:(r.width-(maxX-minX)*z)/2-minX*z,y:(r.height-(maxY-minY)*z)/2-minY*z})}
 function contextMenu(e:React.MouseEvent,h:Host){e.preventDefault();e.stopPropagation();setSelectedHost(h.id);setCtx({x:e.clientX,y:e.clientY,hostId:h.id})}

 const edgeModal=edgeDraft||edgeEdit;
 return <div className="app">
  <aside className="sidebar"><div className="brand"><Network/><div><b>AttackAtlas</b><small>Attack path workspace</small></div></div><div className="side-title">Projects <button onClick={addProject}><Plus size={14}/></button></div><div className="projects">{projects.map(p=><button key={p.id} className={p.id===pid?'active':''} onClick={()=>setPid(p.id)}>{p.name}</button>)}</div><div className="side-title">Assets</div><div className="user-sidebar"><button className="side-action user-toggle" onClick={()=>setUsersExpanded(v=>!v)}><Users/> Users <span>{accounts.length}</span>{usersExpanded?<ChevronDown className="side-chevron"/>:<ChevronRight className="side-chevron"/>}</button>{usersExpanded&&<div className="sidebar-users">{accounts.length===0?<div className="sidebar-empty">No users stored</div>:Array.from(usersByHost.entries()).sort(([a],[b])=>(a??999999)-(b??999999)).map(([hostId,rows])=>{const h=hostId?byId[hostId]:null;return <div className="sidebar-user-group" key={hostId??'unassigned'}><div className="sidebar-user-host">{h?(h.hostname||h.address):'Unassigned'} <span>{rows.length}</span></div>{rows.map(a=><button className="sidebar-user" key={a.id} onClick={()=>editAccount(a)} title="Edit user"><b>{a.domain?`${a.domain}\\`:''}{a.username}</b>{a.notes&&<span>{a.notes}</span>}<Pencil className="sidebar-user-edit"/></button>)}</div>})}<button className="sidebar-manage users-manage" onClick={()=>openCredentials()}><Users/> Manage users</button><label className={`user-import-drop ${userDropHot?'hot':''}`} onDragOver={e=>{e.preventDefault();setUserDropHot(true)}} onDragLeave={()=>setUserDropHot(false)} onDrop={e=>{e.preventDefault();setUserDropHot(false);const f=e.dataTransfer.files?.[0];if(f)importUsersCsv(f)}}><FileUp/><b>Drop users CSV</b><span>username,domain,host,notes</span><small>host = hostname or IP; blank = Unassigned</small><input hidden type="file" accept=".csv,text/csv" onChange={e=>{const f=e.target.files?.[0];if(f)importUsersCsv(f);e.currentTarget.value='' }}/></label></div>}</div><div className="credential-sidebar"><button className="side-action credential-toggle" onClick={()=>setCredentialsExpanded(v=>!v)}><KeyRound/> Credentials <span>{credentials.length}</span>{credentialsExpanded?<ChevronDown className="side-chevron"/>:<ChevronRight className="side-chevron"/>}</button>{credentialsExpanded&&<div className="sidebar-credentials">{credentials.length===0?<div className="sidebar-empty">No credentials stored</div>:credentials.map(c=>{const a=accountById[c.account_id||0],h=c.host_id?byId[c.host_id]:null;return <button key={c.id} className="sidebar-credential" onClick={()=>editCredential(c)} title="Open credential"><b>{a?(a.domain?`${a.domain}\\`: '')+a.username:'Unassigned'}</b><span>{c.kind}{h?` · ${h.hostname||h.address}`:''}</span><code>••••••••</code></button>})}<button className="sidebar-manage" onClick={()=>openCredentials()}><KeyRound/> Manage credentials</button></div>}</div><div className="side-foot">Local only · API v1 · :7843</div></aside>
  <main><header><div><h1>{projects.find(p=>p.id===pid)?.name||'AttackAtlas'}</h1><p>Map systems, services, credentials and attack paths.</p></div><div className="header-actions">{pid&&<button className="project-settings-btn" onClick={openProjectSettings}><Settings/> Project</button>}{pid&&<button className="project-export-btn" onClick={exportMarkdown}><Download/> Markdown</button>}<button className="add-host" onClick={()=>setAddOpen(true)}><Plus/> Add host</button><label className="import"><Upload/> Import Nmap<input hidden type="file" accept=".xml,text/xml" onChange={e=>e.target.files?.[0]&&upload(e.target.files[0])}/></label></div></header>
   <input ref={nmapInputRef} hidden type="file" accept=".xml,text/xml" onChange={e=>{const f=e.target.files?.[0];if(f)upload(f,pendingScanHost.current?byId[pendingScanHost.current]:undefined);e.currentTarget.value=''}}/>
   {msg&&<div className="notice"><span>{msg}</span><button onClick={()=>setMsg('')}><X/></button></div>}
   <div className="toolbar"><button className={mode==='select'?'active':''} onClick={()=>{setMode('select');setConnectFrom(null)}}><MousePointer2/> Select</button><button className={mode==='pan'?'active':''} onClick={()=>{setMode('pan');setConnectFrom(null)}}><Hand/> Pan</button><button className={mode==='connect'?'active':''} onClick={()=>{setMode('connect');setConnectFrom(null)}}><Link2/> Connect</button>{connectFrom&&<span className="connect-hint">Source: {byId[connectFrom]?.hostname||byId[connectFrom]?.address} → choose target</span>}<div className="toolbar-spacer"/>{selectedEdge!==null&&<><button onClick={()=>{const e=edges.find(x=>x.id===selectedEdge);if(e)openEdgeEditor(e)}}><Edit3/> Edit edge</button><button className="danger" onClick={()=>deleteEdge()}><Trash2/> Delete edge</button></>}<button onClick={()=>zoomAt(zoom-.1)}><ZoomOut/></button><span className="zoom-label">{Math.round(zoom*100)}%</span><button onClick={()=>zoomAt(zoom+.1)}><ZoomIn/></button><button onClick={fitView}><Maximize2/> Fit</button><span><Server/> {hosts.length}</span><span><ScanLine/> {totalServices}</span><button onClick={()=>setPanelOpen(v=>!v)}>{panelOpen?<PanelRightClose/>:<PanelRightOpen/>}</button></div>
   <section className={`workspace ${panelOpen&&selected?'with-panel':''}`}>
    <div className="viewport" ref={viewportRef} onPointerDown={viewportPointerDown} onPointerMove={pointerMove} onPointerUp={pointerUp} onPointerCancel={pointerUp} onWheel={e=>{if(e.ctrlKey||e.metaKey){e.preventDefault();zoomAt(zoom+(e.deltaY<0?.08:-.08))}}} onDragEnter={e=>{if(e.dataTransfer.types.includes('Files'))setGlobalDrop(true)}} onDragOver={e=>{if(e.dataTransfer.types.includes('Files')){e.preventDefault();e.dataTransfer.dropEffect='copy'}}} onDragLeave={e=>{if(e.currentTarget===e.target)setGlobalDrop(false)}} onDrop={e=>{if(e.dataTransfer.files?.length){e.preventDefault();setGlobalDrop(false);upload(e.dataTransfer.files[0])}}}>
     <div className="world" style={{width:WORLD_W,height:WORLD_H,transform:`translate(${pan.x}px,${pan.y}px) scale(${zoom})`}}>
      <svg className="edge-layer" width={WORLD_W} height={WORLD_H}><defs><marker id="arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto"><path d="M 0 0 L 10 5 L 0 10 z"/></marker></defs>
       {edges.filter(e=>e.source_type==='host'&&e.target_type==='host'&&byId[e.source_id]&&byId[e.target_id]).map(e=>{const p=edgePath(byId[e.source_id],byId[e.target_id]);return <g key={e.id} className={`edge rel-${e.relation} ${selectedEdge===e.id?'selected':''}`} onClick={ev=>{ev.stopPropagation();setSelectedEdge(e.id);setSelectedHost(null)}} onDoubleClick={ev=>{ev.stopPropagation();openEdgeEditor(e)}}><path className="edge-hit" d={p.d}/><path className="edge-line" d={p.d} markerEnd={e.directed?'url(#arrow)':undefined}/><g className="edge-label" transform={`translate(${p.mx},${p.my})`}><rect x="-72" y="-17" width="144" height="34" rx="8"/><text y="-2">{e.label||e.relation}</text>{e.label&&<text className="edge-sub" y="11">{e.relation}</text>}</g></g>})}
       {connectDrag&&<path className="edge-preview" d={`M ${byId[connectDrag.source]?.pos_x+NODE_W/2||0} ${byId[connectDrag.source]?.pos_y+NODE_H/2||0} L ${connectDrag.x} ${connectDrag.y}`}/>}</svg>
      {hosts.map(h=><article key={h.id} data-host-id={h.id} className={`node os-${h.os_family} ${drag?.id===h.id?'dragging':''} ${hostDrop===h.id?'drop-target':''} ${connectFrom===h.id?'connect-source':''} ${selectedHost===h.id?'selected':''}`} style={{left:h.pos_x,top:h.pos_y,width:NODE_W,minHeight:NODE_H}} onPointerDown={e=>hostPointerDown(e,h)} onDoubleClick={()=>{setSelectedHost(h.id);setPanelOpen(true)}} onContextMenu={e=>contextMenu(e,h)} onDragOver={e=>{if(e.dataTransfer.files?.length){e.preventDefault();e.stopPropagation();setHostDrop(h.id)}}} onDragLeave={()=>setHostDrop(null)} onDrop={e=>{if(e.dataTransfer.files?.length){e.preventDefault();e.stopPropagation();setHostDrop(null);upload(e.dataTransfer.files[0],h)}}}>
       {(['top','right','bottom','left'] as const).map(side=><i key={side} className={`port-handle ${side}`} title="Drag to connect" onPointerDown={e=>startConnector(e,h)} onPointerMove={moveConnector} onPointerUp={endConnector}/>) }
       <div className="node-top"><span className="host-icon"><HostIcon host={h}/></span><div className="identity"><strong title={h.hostname||h.address}>{h.hostname||h.address}</strong><span>{h.address}</span><small>{h.os||h.os_family} · {h.device_type}</small></div><div className="host-actions"><button title="Edit host" onClick={e=>{e.stopPropagation();openHostEditor(h)}}><Pencil/></button><button title="More" onClick={e=>{e.stopPropagation();const r=(e.currentTarget as HTMLElement).getBoundingClientRect();setCtx({x:r.right,y:r.bottom,hostId:h.id})}}><MoreHorizontal/></button><button title="Details" onClick={e=>{e.stopPropagation();setSelectedHost(h.id);setPanelOpen(true)}}><PanelRightOpen/></button><button className="node-delete" title="Delete host" onClick={e=>{e.stopPropagation();deleteHost(h.id)}}><Trash2/></button></div></div>
       <div className="service-pills">{h.services.slice(0,5).map(s=><span key={s.id}>{s.port}</span>)}{h.services.length>5&&<span>+{h.services.length-5}</span>}{!h.services.length&&<span className="empty-pill">Drop Nmap XML here</span>}</div><div className="node-foot"><span className={`status ${h.status}`}>{h.status}</span><span>{h.services.length} services</span><span>{credentials.filter(c=>c.host_id===h.id).length} creds</span></div>
      </article>)}
     </div>
     {globalDrop&&<div className="global-drop"><Upload/><b>Drop Nmap XML</b><span>Import hosts into this project</span></div>}
     <div ref={trashRef} className={`trash-zone ${drag?'visible':''} ${trashHot?'hot':''}`}><Trash2/><b>Delete host</b><span>drag here</span></div><div className="map-controls"><button onClick={()=>zoomAt(zoom-.1)}><ZoomOut/></button><button onClick={()=>zoomAt(zoom+.1)}><ZoomIn/></button><button onClick={fitView}><Maximize2/></button><button onClick={()=>{setZoom(1);setPan({x:0,y:0})}}><RotateCcw/></button></div>
    </div>
    {panelOpen&&selected&&<aside className="inspector"><div className="inspector-head"><div className="inspector-icon"><HostIcon host={selected}/></div><div><small>HOST</small><h2>{selected.hostname||selected.address}</h2><span>{selected.address}</span></div><button onClick={()=>setPanelOpen(false)}><X/></button></div><div className="tabs">{(['details','services','notes','credentials'] as const).map(t=><button key={t} className={tab===t?'active':''} onClick={()=>setTab(t)}>{t}</button>)}</div><div className="inspector-body">
      {tab==='details'&&<><dl><dt>OS family</dt><dd>{selected.os_family}</dd><dt>OS</dt><dd>{selected.os||'—'}</dd><dt>Type</dt><dd>{selected.device_type}</dd><dt>Status</dt><dd><span className={`status ${selected.status}`}>{selected.status}</span></dd><dt>Position</dt><dd>{Math.round(selected.pos_x)}, {Math.round(selected.pos_y)}</dd></dl><section><h3><Activity/> Summary</h3><p className="muted">{selected.services.length} services · {hostCreds.length} credentials · {edges.filter(e=>e.source_id===selected.id||e.target_id===selected.id).length} connections</p></section></>}
      {tab==='services'&&<section className="first-section"><h3><Activity/> Services</h3>{selected.services.length?<div className="service-list service-list-clickable">{selected.services.map(s=>{const open=expandedService===s.id;return <div className={`service-entry ${open?'open':''}`} key={s.id}><button className="service-summary" onClick={()=>setExpandedService(open?null:s.id)} aria-expanded={open}><b>{s.port}/{s.protocol}</b><span><strong>{s.name||'unknown'}</strong>{[s.product,s.version,s.extra_info].filter(Boolean).length?` · ${[s.product,s.version,s.extra_info].filter(Boolean).join(' ')}`:''}</span><ChevronDown className={open?'rotated':''}/></button>{open&&<div className="service-detail"><dl><dt>State</dt><dd>{s.state||'—'}</dd><dt>Service</dt><dd>{s.name||'—'}</dd><dt>Product</dt><dd>{s.product||'—'}</dd><dt>Version</dt><dd>{s.version||'—'}</dd><dt>Extra info</dt><dd>{s.extra_info||'—'}</dd></dl>{s.script_output?<><h4>Nmap script output</h4><pre>{s.script_output}</pre></>:<p className="service-no-output">No NSE script output was recorded for this service.</p>}{s.raw_output?<details className="raw-service-output"><summary>Raw Nmap XML for this port</summary><pre>{s.raw_output}</pre></details>:<p className="service-import-note">Raw scan output is not available for this older import. Re-import the Nmap XML to populate it.</p>}</div>}</div>})}</div>:<p className="muted">No services yet.</p>}</section>}
      {tab==='notes'&&<section className="first-section"><h3><StickyNote/> Notes</h3><p className="note-box">{selected.notes||'No notes yet.'}</p><button className="inline-action" onClick={()=>editNotes(selected)}><Edit3/> Edit notes</button></section>}
      {tab==='credentials'&&<section className="first-section"><h3><KeyRound/> Credentials found here</h3>{hostCreds.length?<div className="host-credential-list">{hostCreds.map(c=>{const a=accountById[c.account_id||0],show=showSecrets.has(c.id);return <div className="host-credential-card" key={c.id}><div className="host-credential-head"><div><b>{a?(a.domain?`${a.domain}\\`: '')+a.username:'Unassigned'}</b><span>{c.kind} · {c.source}</span></div><button title="Edit credential" onClick={()=>editCredential(c)}><Pencil/></button></div><div className="host-credential-secret"><code title={show?c.secret:'Secret hidden'}>{show?c.secret:'••••••••••••'}</code><div className="host-credential-actions"><button title={show?'Hide secret':'Show secret'} onClick={()=>toggleSecret(c.id)}>{show?<EyeOff/>:<Eye/>}<span>{show?'Hide':'Show'}</span></button><button title="Copy secret" onClick={()=>copySecret(c.secret)}><Copy/><span>Copy</span></button></div></div>{c.notes&&<p>{c.notes}</p>}</div>})}</div>:<p className="muted">No credentials linked to this host.</p>}<button className="inline-action" onClick={()=>openCredentials(selected.id)}><Plus/> Add credential</button></section>}
      <section className="inspector-actions"><button onClick={()=>openHostEditor(selected)}><Pencil/> Edit host</button><button onClick={()=>triggerScan(selected.id)}><Upload/> Add Nmap scan</button><button onClick={()=>{setMode('connect');setConnectFrom(selected.id)}}><ArrowRight/> Connect from host</button><button onClick={()=>openCredentials(selected.id)}><KeyRound/> Manage credentials</button><button onClick={()=>editNotes(selected)}><StickyNote/> Edit notes</button><button className="danger" onClick={()=>deleteHost(selected.id)}><Trash2/> Delete host</button></section>
    </div></aside>}
   </section>
  </main>
  {ctx&&byId[ctx.hostId]&&<div className="context-menu" style={{left:ctx.x,top:ctx.y}} onClick={e=>e.stopPropagation()}><div className="context-title">{byId[ctx.hostId].hostname||byId[ctx.hostId].address}</div><button onClick={()=>openHostEditor(byId[ctx.hostId])}><Pencil/> Edit host</button><button onClick={()=>{setMode('connect');setConnectFrom(ctx.hostId);setCtx(null)}}><Link2/> Add connection <ChevronRight/></button><button onClick={()=>editNotes(byId[ctx.hostId])}><StickyNote/> Edit notes</button><button onClick={()=>{triggerScan(ctx.hostId);setCtx(null)}}><Upload/> Add Nmap scan</button><button onClick={()=>openCredentials(ctx.hostId)}><KeyRound/> Add credential</button><button onClick={()=>{setSelectedHost(ctx.hostId);setPanelOpen(true);setCtx(null)}}><PanelRightOpen/> Open details</button><div className="context-sep"/><button className="danger" onClick={()=>{deleteHost(ctx.hostId);setCtx(null)}}><Trash2/> Delete host</button></div>}
  {projectOpen&&<div className="modal-backdrop" onMouseDown={()=>setProjectOpen(false)}><form className="modal project-modal" onSubmit={saveProject} onMouseDown={e=>e.stopPropagation()}><div className="modal-head"><div><h2>Project settings</h2><p>Rename the project, keep engagement notes, export it or delete it.</p></div><button type="button" onClick={()=>setProjectOpen(false)}><X/></button></div><label>Project name<input autoFocus required value={projectDraft.name} onChange={e=>setProjectDraft({...projectDraft,name:e.target.value})}/></label><label>Description<input value={projectDraft.description} onChange={e=>setProjectDraft({...projectDraft,description:e.target.value})} placeholder="Short project description"/></label><label>Project notes<textarea rows={10} value={projectDraft.notes} onChange={e=>setProjectDraft({...projectDraft,notes:e.target.value})} placeholder="Scope, objectives, VPN details, assumptions, reminders, findings…"/></label><div className="project-export-note"><FileText/><div><b>Markdown export</b><span>Exports hosts, services, Nmap output, accounts, credentials, attack paths and scan history as a ZIP of Markdown files.</span><small>Credential secrets are included in plaintext.</small></div></div><div className="project-modal-actions"><button type="button" className="danger secondary" onClick={deleteProject}><Trash2/> Delete project</button><div className="modal-actions compact"><button type="button" className="secondary" onClick={exportMarkdown}><Download/> Export Markdown</button><button className="primary"><Save/> Save project</button></div></div></form></div>}
  {addOpen&&<div className="modal-backdrop" onMouseDown={()=>setAddOpen(false)}><form className="modal host-modal" onSubmit={createHost} onMouseDown={e=>e.stopPropagation()}><div className="modal-head"><div><h2>Add host</h2><p>Start from a template or create a custom endpoint.</p></div><button type="button" onClick={()=>setAddOpen(false)}><X/></button></div><label>Host template<select value={templateId} onChange={e=>applyTemplate(e.target.value)}>{hostTemplates.map(t=><option key={t.id} value={t.id}>{t.label}</option>)}</select><small className="field-help">Templates prefill OS family, device type and OS/version. Everything can still be edited.</small></label><div className="form-row"><label>IP / address<input required autoFocus value={draft.address} onChange={e=>setDraft({...draft,address:e.target.value})} placeholder="10.10.10.10"/></label><label>Hostname<input value={draft.hostname} onChange={e=>setDraft({...draft,hostname:e.target.value})} placeholder="DC01"/></label></div><div className="form-row"><label>OS family<select value={draft.os_family} onChange={e=>setDraft({...draft,os_family:e.target.value})}><option value="unknown">Unknown</option><option value="windows">Windows</option><option value="linux">Linux</option><option value="macos">macOS</option><option value="bsd">BSD</option><option value="network">Network OS</option></select></label><label>Device type<select value={draft.device_type} onChange={e=>setDraft({...draft,device_type:e.target.value})}><option value="host">Generic host</option><option value="server">Server</option><option value="workstation">Workstation</option><option value="network">Network device</option><option value="container">Container</option><option value="other">Other</option></select></label></div><div className="form-row"><label>OS / version<input value={draft.os} onChange={e=>setDraft({...draft,os:e.target.value})} placeholder="Windows Server 2022"/></label><label>Status<select value={draft.status} onChange={e=>setDraft({...draft,status:e.target.value})}><option value="discovered">Discovered</option><option value="tested">Tested</option><option value="access">Access</option><option value="admin">Admin</option><option value="owned">Owned</option></select></label></div><label>Notes<textarea rows={3} value={draft.notes} onChange={e=>setDraft({...draft,notes:e.target.value})}/></label><div className="modal-actions"><button type="button" className="secondary" onClick={()=>setAddOpen(false)}>Cancel</button><button className="primary">Add host</button></div></form></div>}
  {editHostId!==null&&<div className="modal-backdrop" onMouseDown={()=>setEditHostId(null)}><form className="modal host-modal" onSubmit={saveHostEdit} onMouseDown={e=>e.stopPropagation()}><div className="modal-head"><div><h2>Edit host</h2><p>Rename imported hosts and correct endpoint metadata at any time.</p></div><button type="button" onClick={()=>setEditHostId(null)}><X/></button></div><div className="form-row"><label>IP / address<input required value={editDraft.address} onChange={e=>setEditDraft({...editDraft,address:e.target.value})}/></label><label>Hostname<input autoFocus value={editDraft.hostname} onChange={e=>setEditDraft({...editDraft,hostname:e.target.value})} placeholder="DC01"/></label></div><div className="form-row"><label>OS family<select value={editDraft.os_family} onChange={e=>setEditDraft({...editDraft,os_family:e.target.value})}><option value="unknown">Unknown</option><option value="windows">Windows</option><option value="linux">Linux</option><option value="macos">macOS</option><option value="bsd">BSD</option><option value="network">Network OS</option></select></label><label>Device type<select value={editDraft.device_type} onChange={e=>setEditDraft({...editDraft,device_type:e.target.value})}><option value="host">Generic host</option><option value="server">Server</option><option value="workstation">Workstation</option><option value="network">Network device</option><option value="container">Container</option><option value="other">Other</option></select></label></div><div className="form-row"><label>OS / version<input value={editDraft.os} onChange={e=>setEditDraft({...editDraft,os:e.target.value})}/></label><label>Status<select value={editDraft.status} onChange={e=>setEditDraft({...editDraft,status:e.target.value})}><option value="discovered">Discovered</option><option value="tested">Tested</option><option value="access">Access</option><option value="admin">Admin</option><option value="owned">Owned</option></select></label></div><label>Notes<textarea rows={4} value={editDraft.notes} onChange={e=>setEditDraft({...editDraft,notes:e.target.value})}/></label><div className="modal-actions"><button type="button" className="secondary" onClick={()=>setEditHostId(null)}>Cancel</button><button className="primary"><Save/> Save host</button></div></form></div>}
  {edgeModal&&<div className="modal-backdrop"><form className="modal edge-modal" onSubmit={edgeEdit?updateEdge:createEdge}><div className="modal-head"><div><h2>{edgeEdit?'Edit attack path':'Create attack path'}</h2><p>{edgeDraft?`${byId[edgeDraft.source]?.hostname||byId[edgeDraft.source]?.address} → ${byId[edgeDraft.target]?.hostname||byId[edgeDraft.target]?.address}`:'Change label, type and direction.'}</p></div><button type="button" onClick={()=>{setEdgeDraft(null);setEdgeEdit(null)}}><X/></button></div><label>Relation<select value={edgeRelation} onChange={e=>setEdgeRelation(e.target.value)}>{relations.map(r=><option key={r} value={r}>{r.replaceAll('_',' ')}</option>)}</select></label><label>Label<input value={edgeLabel} onChange={e=>setEdgeLabel(e.target.value)} placeholder="svc_backup / WinRM"/></label><label className="check"><input type="checkbox" checked={edgeDirected} onChange={e=>setEdgeDirected(e.target.checked)}/> Directed arrow</label><div className="edge-preview-row"><span className={`edge-swatch rel-${edgeRelation}`}/><span>Style follows relation type automatically</span></div><div className="modal-actions">{edgeEdit&&<button type="button" className="danger secondary" onClick={()=>deleteEdge(edgeEdit.id)}><Trash2/> Delete</button>}<button type="button" className="secondary" onClick={()=>{setEdgeDraft(null);setEdgeEdit(null)}}>Cancel</button><button className="primary"><Save/> {edgeEdit?'Save':'Create path'}</button></div></form></div>}
  {noteHost!==null&&<div className="modal-backdrop"><form className="modal" onSubmit={saveNotes}><div className="modal-head"><div><h2>Edit notes</h2><p>{byId[noteHost]?.hostname||byId[noteHost]?.address}</p></div><button type="button" onClick={()=>setNoteHost(null)}><X/></button></div><label>Notes<textarea autoFocus rows={12} value={noteText} onChange={e=>setNoteText(e.target.value)} placeholder="Enumeration, findings, commands, hypotheses…"/></label><div className="modal-actions"><button type="button" className="secondary" onClick={()=>setNoteHost(null)}>Cancel</button><button className="primary"><Save/> Save notes</button></div></form></div>}
  {credOpen&&<div className="modal-backdrop"><div className="modal credential-modal"><div className="modal-head"><div><h2>Credential manager</h2><p>{credHost?`Linked to ${byId[credHost]?.hostname||byId[credHost]?.address}`:'Accounts and secrets for this project.'}</p></div><button onClick={()=>setCredOpen(false)}><X/></button></div><div className="credential-grid"><form className={`credential-create ${editingAccountId?'editing':''}`} onSubmit={createAccount}><h3><UserPlus/> {editingAccountId?'Edit user':'Add user'}</h3><label>Username<input required value={newAccount.username} onChange={e=>setNewAccount({...newAccount,username:e.target.value})}/></label><label>Domain<input value={newAccount.domain} onChange={e=>setNewAccount({...newAccount,domain:e.target.value})} placeholder="CORP"/></label><label>Found on<select value={newAccount.host_id} onChange={e=>setNewAccount({...newAccount,host_id:e.target.value})}><option value="">Unassigned</option>{hosts.map(h=><option key={h.id} value={h.id}>{h.hostname||h.address}</option>)}</select></label><label>Notes<input value={newAccount.notes} onChange={e=>setNewAccount({...newAccount,notes:e.target.value})}/></label><div className="credential-form-actions">{editingAccountId&&<button type="button" className="secondary" onClick={cancelAccountEdit}>Cancel edit</button>}<button className={editingAccountId?'primary':'secondary'}>{editingAccountId?<Save/>:null}{editingAccountId?'Save changes':'Add user'}</button></div></form><form className={`credential-create ${editingCredentialId?'editing':''}`} onSubmit={createCredential}><h3><KeyRound/> {editingCredentialId?'Edit credential':'Add credential'}</h3><label>Account<select value={newCred.account_id} onChange={e=>setNewCred({...newCred,account_id:e.target.value})}><option value="">Unassigned</option>{accounts.map(a=><option key={a.id} value={a.id}>{a.domain?`${a.domain}\\`:''}{a.username}</option>)}</select></label><div className="form-row"><label>Type<select value={newCred.kind} onChange={e=>setNewCred({...newCred,kind:e.target.value})}><option>password</option><option>ntlm</option><option>hash</option><option>ssh_key</option><option>token</option><option>api_key</option><option>cookie</option><option>other</option></select></label><label>Found on<select value={newCred.host_id} onChange={e=>setNewCred({...newCred,host_id:e.target.value})}><option value="">Unknown</option>{hosts.map(h=><option key={h.id} value={h.id}>{h.hostname||h.address}</option>)}</select></label></div><label>Secret<input required value={newCred.secret} onChange={e=>setNewCred({...newCred,secret:e.target.value})} autoComplete="off"/></label><label>Source<input value={newCred.source} onChange={e=>setNewCred({...newCred,source:e.target.value})} placeholder="manual / smb / config"/></label><label>Notes<input value={newCred.notes} onChange={e=>setNewCred({...newCred,notes:e.target.value})}/></label><div className="credential-form-actions">{editingCredentialId&&<button type="button" className="secondary" onClick={cancelCredentialEdit}>Cancel edit</button>}<button className="primary"><Save/> {editingCredentialId?'Save changes':'Add credential'}</button></div></form></div><div className="credential-list"><div className="credential-list-head"><b>Stored credentials</b><span>{credentials.length}</span></div>{credentials.length===0?<p className="muted">No credentials yet.</p>:credentials.map(c=>{const a=accountById[c.account_id||0],h=c.host_id?byId[c.host_id]:null,show=showSecrets.has(c.id);return <div className={`credential-row ${editingCredentialId===c.id?'is-editing':''}`} key={c.id}><div className="credential-identity"><b>{a?(a.domain?`${a.domain}\\`: '')+a.username:'Unassigned'}</b><span>{c.kind} · {h?`found on ${h.hostname||h.address}`:c.source}</span>{c.notes&&<small>{c.notes}</small>}</div><code>{show?c.secret:'••••••••••••'}</code><div className="credential-row-actions"><button title="Edit" onClick={()=>editCredential(c)}><Pencil/></button><button title={show?'Hide':'Show'} onClick={()=>toggleSecret(c.id)}>{show?<EyeOff/>:<Eye/>}</button><button title="Copy" onClick={()=>copySecret(c.secret)}><Copy/></button><button className="danger-icon" title="Delete" onClick={()=>deleteCredential(c.id)}><Trash2/></button></div></div>})}</div></div></div>}
 </div>
}
createRoot(document.getElementById('root')!).render(<App/>);
